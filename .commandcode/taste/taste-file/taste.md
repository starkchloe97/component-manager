# Taste File
- Uses Vue 3 reactive CSS custom properties via `v-bind()` inside `<style scoped>` blocks, paired with shared composables (e.g., `useComponentStyles`) to create runtime-editable theming per component. **Critical**: CSS rules must actually reference the reactive variables via `var()` — merely declaring `v-bind` without using the variables in CSS rules results in non-functional hardcoding. Confidence: 0.9
- The `useComponentStyles` composable uses a specific variable naming convention: `primary`, `background`, `pdtop`, `pdbottom`, `pdleft`, `pdright`, `fontSize`, `fontSizeBody`, `surface`, `muted` — which map to CSS custom properties `--primary`, `--bg`, `--pd-top`, `--pd-bottom`, `--pd-left`, `--pd-right`, `--font-size`, `--font-size-body`, `--surface`, `--body-txt-color`. All components sharing this system must use identical variable names for consistency. Confidence: 0.85
- Standard component setup includes both `useScrollReveal` (with a `sectionRef`) and `useComponentStyles` — both are expected together for scroll-reactive themed components. Confidence: 0.8
- When extending a pattern across multiple components, expects functional equivalence — not just code similarity — to a reference component. Confidence: 0.88
- When converting components to use a reactive variable system, ALL hardcoded values must be replaced — including not only literal hex colors and pixel values, but also references to other non-reactive CSS variables (e.g., `var(--text-secondary)`, `var(--on-surface)`, `var(--bg-card)`). Replacing only literals while leaving other variable refs is considered incomplete and unacceptable. Confidence: 0.85
- Expects systematic, exhaustive conversion across the entire component set rather than spot-fixing; partial fixes where some components still have hardcoded values while others don't are flagged as not done. Confidence: 0.82
- Prefers focused, minimal changes; does not want extra or unrelated modifications bundled into a task. Confidence: 0.75
- When wiring components into a reactive variable system, expects the reactive variables to be mapped to whatever CSS variables each component already uses (e.g., `--brand`, `--on-surface`, `--text-secondary`, `--bg-card`) rather than replacing all hardcoded values with a single shared set of variable names. Each component's local CSS variables should be rewired to point to the reactive sources. Confidence: 0.85
- Expects reactive CSS variables to actually drive styling — merely declaring `v-bind` without wiring the variables into CSS rules via `var()` is considered non-functional and unacceptable. Confidence: 0.85
- Gives direct, goal-oriented instructions referencing specific component names and desired outcomes. Confidence: 0.7
